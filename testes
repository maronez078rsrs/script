--[[
    REAL XITERS V8.0 - ESTÁVEL (FIXED HITBOX & SPEED)
    OWNER: Ysatwj
]]

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera
local Mouse = LocalPlayer:GetMouse()

-- CONFIGURAÇÃO DE DONO
local OwnerNick = "ysatwj"

-- BANCO DE LOGINS (CORRIGIDO)
local LoginsPremium = {
    ["LNHAHA"] = "LNHAHA",
    ["zk"] = "zk777",
}

local LoginsFree = {
    ["Luka"] = "FREE-HAHAH-KAJZS",
    ["Guest"] = "FREE"
}

_G.Config = {
    AimbotActive = false, 
    AimbotKey = Enum.KeyCode.E, 
    FOVSize = 150,
    HitboxActive = false, 
    HitboxSize = 2,
    SpeedActive = false, 
    SpeedValue = 16,
    IsFreeVersion = false
}

-- FUNÇÃO DE ALVO
local function GetClosestToMouse()
    local Target = nil
    local MaxDist = _G.Config.FOVSize
    for _, v in pairs(Players:GetPlayers()) do
        if v.Name ~= OwnerNick and v ~= LocalPlayer and v.Character and v.Character:FindFirstChild("Head") then
            local Pos, OnScreen = Camera:WorldToViewportPoint(v.Character.Head.Position)
            if OnScreen then
                local MousePos = Vector2.new(Mouse.X, Mouse.Y)
                local Dist = (MousePos - Vector2.new(Pos.X, Pos.Y)).Magnitude
                if Dist < MaxDist then 
                    Target = v
                    MaxDist = Dist 
                end
            end
        end
    end
    return Target
end

-- INTERFACE PRINCIPAL
local function OpenMainUI()
    local ScreenGui = Instance.new("ScreenGui", LocalPlayer.PlayerGui)
    local Main = Instance.new("Frame", ScreenGui)
    Main.Size = UDim2.new(0, 350, 0, 400)
    Main.Position = UDim2.new(0.5, -175, 0.5, -200)
    Main.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
    Main.Active = true
    Main.Draggable = true
    Instance.new("UICorner", Main)

    local Title = Instance.new("TextLabel", Main)
    Title.Text = "REAL XITERS - MENU"
    Title.Size = UDim2.new(1, 0, 0, 40)
    Title.TextColor3 = Color3.fromRGB(130, 0, 255)
    Title.Font = Enum.Font.GothamBold
    Title.TextSize = 18
    Title.BackgroundTransparency = 1

    local Container = Instance.new("ScrollingFrame", Main)
    Container.Size = UDim2.new(1, -20, 1, -60)
    Container.Position = UDim2.new(0, 10, 0, 50)
    Container.BackgroundTransparency = 1
    Container.ScrollBarThickness = 2

    local layout = Instance.new("UIListLayout", Container)
    layout.Padding = UDim.new(0, 10)

    local function CreateToggle(text, configKey)
        local btn = Instance.new("TextButton", Container)
        btn.Size = UDim2.new(1, 0, 0, 40)
        btn.Text = text .. ": OFF"
        btn.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
        btn.TextColor3 = Color3.new(1, 1, 1)
        Instance.new("UICorner", btn)

        btn.MouseButton1Click:Connect(function()
            _G.Config[configKey] = not _G.Config[configKey]
            btn.Text = text .. ": " .. (_G.Config[configKey] and "ON" or "OFF")
            btn.BackgroundColor3 = _G.Config[configKey] and Color3.fromRGB(130, 0, 255) or Color3.fromRGB(30, 30, 30)
        end)
    end

    local function CreateSlider(text, min, max, configKey)
        local frame = Instance.new("Frame", Container)
        frame.Size = UDim2.new(1, 0, 0, 50)
        frame.BackgroundTransparency = 1

        local label = Instance.new("TextLabel", frame)
        label.Text = text .. ": " .. _G.Config[configKey]
        label.Size = UDim2.new(1, 0, 0, 20)
        label.TextColor3 = Color3.new(1, 1, 1)
        label.BackgroundTransparency = 1

        local sliderBg = Instance.new("Frame", frame)
        sliderBg.Size = UDim2.new(0.9, 0, 0, 5)
        sliderBg.Position = UDim2.new(0.05, 0, 0.7, 0)
        sliderBg.BackgroundColor3 = Color3.fromRGB(50, 50, 50)

        local sliderFill = Instance.new("Frame", sliderBg)
        sliderFill.Size = UDim2.new((_G.Config[configKey]-min)/(max-min), 0, 1, 0)
        sliderFill.BackgroundColor3 = Color3.fromRGB(130, 0, 255)

        sliderBg.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 then
                local connection
                connection = RunService.RenderStepped:Connect(function()
                    local percent = math.clamp((Mouse.X - sliderBg.AbsolutePosition.X) / sliderBg.AbsoluteSize.X, 0, 1)
                    sliderFill.Size = UDim2.new(percent, 0, 1, 0)
                    local value = math.floor(min + (max - min) * percent)
                    _G.Config[configKey] = value
                    label.Text = text .. ": " .. value
                end)
                UserInputService.InputEnded:Connect(function(ended)
                    if ended.UserInputType == Enum.UserInputType.MouseButton1 then
                        if connection then connection:Disconnect() end
                    end
                end)
            end
        end)
    end

    CreateToggle("AIMBOT (E)", "AimbotActive")
    CreateToggle("EXPAND HITBOX", "HitboxActive")
    CreateSlider("HITBOX SIZE", 2, 50, "HitboxSize")
    CreateToggle("SPEED HACK", "SpeedActive")
    CreateSlider("SPEED VALUE", 16, 300, "SpeedValue")
end

-- TELA DE LOGIN
local function CreateLogin()
    local gui = Instance.new("ScreenGui", LocalPlayer.PlayerGui)
    local main = Instance.new("Frame", gui)
    main.Size = UDim2.new(0, 300, 0, 300)
    main.Position = UDim2.new(0.5, -150, 0.5, -150)
    main.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
    Instance.new("UICorner", main)

    local user = Instance.new("TextBox", main)
    user.PlaceholderText = "USUÁRIO"
    user.Size = UDim2.new(0.8, 0, 0, 40)
    user.Position = UDim2.new(0.1, 0, 0.2, 0)
    user.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
    user.TextColor3 = Color3.new(1,1,1)

    local pass = Instance.new("TextBox", main)
    pass.PlaceholderText = "SENHA"
    pass.Size = UDim2.new(0.8, 0, 0, 40)
    pass.Position = UDim2.new(0.1, 0, 0.45, 0)
    pass.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
    pass.TextColor3 = Color3.new(1,1,1)

    local btn = Instance.new("TextButton", main)
    btn.Text = "LOGAR"
    btn.Size = UDim2.new(0.8, 0, 0, 50)
    btn.Position = UDim2.new(0.1, 0, 0.7, 0)
    btn.BackgroundColor3 = Color3.fromRGB(130, 0, 255)
    btn.TextColor3 = Color3.new(1,1,1)

    btn.MouseButton1Click:Connect(function()
        if (LoginsPremium[user.Text] == pass.Text or LoginsFree[user.Text] == pass.Text) and user.Text ~= "" then
            gui:Destroy()
            OpenMainUI()
        else
            btn.Text = "ERRO"
            task.wait(1)
            btn.Text = "LOGAR"
        end
    end)
end

-- LOOP PRINCIPAL
RunService.RenderStepped:Connect(function()
    if _G.Config.AimbotActive and UserInputService:IsKeyDown(_G.Config.AimbotKey) then
        local t = GetClosestToMouse()
        if t and t.Character and t.Character:FindFirstChild("Head") then 
            Camera.CFrame = CFrame.new(Camera.CFrame.Position, t.Character.Head.Position) 
        end
    end

    if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
        if _G.Config.SpeedActive then
            LocalPlayer.Character.Humanoid.WalkSpeed = _G.Config.SpeedValue
        else
            LocalPlayer.Character.Humanoid.WalkSpeed = 16
        end
    end

    if _G.Config.HitboxActive then
        for _, v in pairs(Players:GetPlayers()) do
            if v ~= LocalPlayer and v.Name ~= OwnerNick and v.Character and v.Character:FindFirstChild("Head") then
                local head = v.Character.Head
                head.Size = Vector3.new(_G.Config.HitboxSize, _G.Config.HitboxSize, _G.Config.HitboxSize)
                head.Transparency = 0.7
                head.Color = Color3.fromRGB(130, 0, 255)
                head.CanCollide = false
                
                if head:FindFirstChildOfClass("SpecialMesh") then
                    head:FindFirstChildOfClass("SpecialMesh"):Destroy()
                end
            end
        end
    end
end)

CreateLogin()
